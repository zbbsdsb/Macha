# 未定决策（open decisions）—— 路径一：SepMay / IVY 生命周期

> **STATUS: OPEN —— 明确不阻塞任何实现。** 原为路径文档 §5 的 parking lot，抽出来单独维护，
> 以便回答"到什么条件下必须回答它"。
> 路径本体：[`README.md`](README.md) ｜ 评估：[`eval/2026-09-14-evaluation.md`](eval/2026-09-14-evaluation.md)

---

## 三条硬规矩

1. **未定项不阻塞**：任何一条未定，都不构成"不能推进 Layer"的理由。
2. **不猜**：未定项如果要被"临时猜一个"，必须显式标注为临时假设，并写明作废条件。
3. **到点必须回答**：每条下面都写了"何时必须回答"；到点仍未回答，则该路径**不得解冻**。

---

## D1 · 寿命类怎么分档 ★ 最高优先

- **问题**：Chunk 的"寿命类"按什么切分？（一次相遇 / 一天 / 一段关系 / 别的切法。）
- **为什么卡**：它是 Chunk **关闭时机**的唯一依据。不定它，"关闭时回流"无从发生。
- **现状倾向**：未定。倾向按"叙事可辨的边界"切（玩家能感到的段落），而不是按挂钟时间。
- **何时必须回答**：**路径解冻前**（否则解冻出来的东西无法关闭）。
- **谁来答**：用户（这是设计判断，不是实现选择）。

## D2 · 分组标准由谁声明、怎么声明

- **问题**：特性集（参与者集/活动类/寿命类/资源类）由谁定义？Thread 的标签由谁贴？
- **现状倾向**：作者定义**闭合枚举**；标签由创建 Thread 的一方贴；**无标签不猜**——落到默认 Chunk
  并报一条可见的错，由作者决定是否新增枚举值。
- **判据（用来定夺）**：**出错时该谁付费。** 推断式分组出错 → 玩家付费；声明式出错 → 设计者付费。
  数据层应让设计者付费。
- **何时必须回答**：路径解冻时（与 D1 同时）。
- **谁来答**：用户 + 实现方共同（枚举内容偏设计，报错行为偏实现）。

## D3 · "语义调度器"是否成立

- **问题**：Chunk 由"专门的语义调度器"划定（原案自标存疑）。
- **本轮立场**：**反对**把"语义"放进调度器。它一旦需要理解情境，就成了第四个认知体——
  谁授权它解释世界？必然晚一步；而且无法测试。主张**声明式匹配 + 装箱**。
- **反驳条件（能推翻我）**：举出一个真实场景，其中"这两条 Thread 该归为一组"**无法**用声明的标签
  表达、必须靠运行期语义推断。举得出来，则调度器确实需要语义。
- **何时必须回答**：路径解冻前。
- **谁来答**：用户（举证或采纳立场）。

## D4 · 权重更新的语义 ★ 风险最高

- **问题**：回流三路里的"权重"，是**数值自适应**，还是**写一条可读结论**？
- **为什么重要**：这是**人格漂移与假记忆的唯一产地**——整条路径的生死线。
- **现状倾向**：未定。技术上是"有界、可回放、可撤回"的约束先于表示形式。
- **何时必须回答**：**最早也要在写最小可证伪版本之前**；若届时无法给出"漂移可被检出"的判据，
  按路径 §9 的反对意见处理——**整条放弃**，而不是继续加机制。
- **谁来答**：用户 + 实现方。

## D5 · 路径二、路径三是什么

- **问题**：用户提出"我们需要多个路径"，但目前只存在路径一。
- **为什么是决策**：一条路径的评分是**相对**的——没有对照，6.5 分无法判断是高是低。
- **现状倾向**：等待用户给出路径二（哪怕是草图），或明确宣布"只走一条"。
- **何时必须回答**：不阻塞；但"路径一是否值得投入"这个判断在被问到之前，先按 §评估的绝对分处理。
- **谁来答**：用户。

## D6 · 与 Layer 协议的关系（预留，非阻塞）

- **问题**：`action_result(pending)`、observation 的节奏/压力事实、action 的 deadline/budget，
  要不要进协议 v0？
- **现状处置**：**只占位、不实现、不冻结**，已写在
  [`../../plans/minecraft-layer/01-protocol-v0.md`](../../plans/minecraft-layer/01-protocol-v0.md) §9 的"预留"说明里。
- **何时必须回答**：Layer 的 8 步闭环跑完、拿到实测之后（即 P3 的冻结时点）。
- **谁来答**：Layer 侧实现方 + 用户。

---

## 与解冻条件的关系

路径一的解冻条件见其 [`README.md`](README.md) §6（三条满足任一）。其中"② 出现真实场景逼问寿命类分档"
= **D1 被现实提出**；"③ 愿意写最小可证伪版本"要求 **D4 至少给出可检出的漂移判据**。
换句话说：**D1 与 D4 是这条路径真正的门。**
