# Examples

Reproducible case studies for the paper — the "living case set" that backs 岔路 4
(the 3–5 game-NPC validations of the relationship language) in
`papers/notes/05-macha-question-tree.md`.

**Purpose:** each example isolates as few relationship axes as possible (e.g. memory-only,
selfhood-only), so a single-axis difference can prove that axis is real and
system-irreplaceable.

Placeholder — no cases written yet. Keep each case self-contained and tie it back to the
relationship-gap dimensions it exercises.